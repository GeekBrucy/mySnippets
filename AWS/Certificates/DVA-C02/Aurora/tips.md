- improve the read scaling of the database
  - Create Aurora Replicas in same cluster as the primary database instance.
  - Create a separate Aurora MySQL cluster and configure binlog replication.

- binlog?